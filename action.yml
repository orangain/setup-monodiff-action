name: "Setup monodiff"
description: "Set up a specific version of monodiff and add it to the PATH"
inputs:
  version:
    description: "Version of monodiff"
    required: false

runs:
  using: "node12"
  main: "dist/index.js"
